@model IEnumerable<TrashCollector.Models.Day>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.DayOfWeek }) |
                @Html.ActionLink("Details", "Details", new { id=item.DayOfWeek }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.DayOfWeek })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
